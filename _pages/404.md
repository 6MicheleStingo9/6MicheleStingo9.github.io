---
layout: page
permalink: /404.html
title: "Page not found"
description: "Nothing at this address. Not everything that goes missing turns up again."
# No `redirect: true`: that machinery emits a fixed <meta refresh> to the site
# root, which threw a reader who mistyped an /it/ address onto the English home.
# GitHub Pages serves this one page for every missing path, so the language can
# only be read from the address at run time, below.
---

You will be redirected to the main page within 3 seconds. If not redirected, please go back to the <a id="missing-home" href="{{ site.baseurl | append: '/' }}">home page</a>.

<noscript>
  <meta http-equiv="refresh" content="3; url={{ site.baseurl | append: '/' }}">
</noscript>

<!-- prettier-ignore -->
<script>
  (function () {
    var base = {{ site.baseurl | jsonify }} || '';
    var target = base + '/';
    if (window.location.pathname.indexOf(base + '/it/') === 0) {
      target = base + '/it/';
    }
    var link = document.getElementById('missing-home');
    if (link) {
      link.setAttribute('href', target);
    }
    window.setTimeout(function () {
      window.location.replace(target);
    }, 3000);
  })();
</script>
